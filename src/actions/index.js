export const addTask = (data) => (
    {type: "ADD", data}
);

export const updateTask = (data) => (
    {type: "UPDATE", data}
);

export const deleteTask = (data) => (
    {type: "DELETE", data}
);
